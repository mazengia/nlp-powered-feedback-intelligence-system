apiVersion: v1
kind: Service
metadata:
  name: feedback-analyzer
  namespace: feedback-system
  labels:
    app: feedback-analyzer
spec:
  type: ClusterIP
  selector:
    app: feedback-analyzer
  ports:
  - name: http
    port: 8080
    targetPort: 8080
    protocol: TCP
  sessionAffinity: None

